import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Preferences | Water Monitoring System",
};

export default function Preferences() {
  return (
    <div>
      <h1>Preferences</h1>
    </div>
  );
}